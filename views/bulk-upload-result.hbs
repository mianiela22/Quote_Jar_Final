<div class="container">
    <div class="page-header">
        <h1>✅ Upload Complete!</h1>
    </div>

    <div class="upload-results">
        <div class="result-card success">
            <h2>{{successCount}}</h2>
            <p>Quotes Successfully Added</p>
        </div>
        
        {{#if errorCount}}
        <div class="result-card error">
            <h2>{{errorCount}}</h2>
            <p>Quotes Failed (check format)</p>
        </div>
        {{/if}}
    </div>

    <div style="text-align: center; margin-top: 3rem;">
        <a href="/home" class="btn btn-primary btn-large" style="max-width: 300px;">View My Quotes</a>
        <a href="/bulk-upload" class="btn btn-secondary btn-large" style="max-width: 300px;">Upload More</a>
    </div>
</div>

<style>
.upload-results {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin: 3rem 0;
}

.result-card {
    text-align: center;
    padding: 3rem 2rem;
    border-radius: 12px;
    border: 2px solid #e5e5e5;
}

.result-card.success {
    background: #f0fdf4;
    border-color: #10b981;
}

.result-card.error {
    background: #fef2f2;
    border-color: #ef4444;
}

.result-card h2 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
}

.result-card.success h2 {
    color: #10b981;
}

.result-card.error h2 {
    color: #ef4444;
}

.result-card p {
    color: #666;
    font-size: 1.1rem;
}
</style>