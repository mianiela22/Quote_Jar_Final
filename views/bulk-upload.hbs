<div class="container">
    <div class="page-header">
        <h1>📤 Bulk Upload Quotes</h1>
        <a href="/home" class="btn btn-secondary">← Back to Home</a>
    </div>

    <div class="bulk-upload-instructions">
        <h3>How to format your quotes:</h3>
        <p>Paste your quotes with one per line. Supported formats:</p>
        <ul>
            <li><code>"Quote text" -Person Name</code></li>
            <li><code>Quote text -Person Name</code></li>
        </ul>
        <p><strong>Example:</strong></p>
        <pre>"I think God was asserting dominance" -Mirla
Jesus is cool. -Anya
We are new borns -Tracy</pre>
    </div>

    <form action="/bulk-upload" method="POST" class="quote-form">
        <div class="form-group">
            <label for="quotesText">Paste Your Quotes Below</label>
            <textarea 
                id="quotesText" 
                name="quotesText" 
                rows="15" 
                required
                placeholder="Paste your quotes here, one per line..."
            ></textarea>
        </div>

        <button type="submit" class="btn btn-primary btn-large">📤 Upload All Quotes</button>
    </form>
</div>

<style>
.bulk-upload-instructions {
    background: #f5f5f5;
    padding: 1.5rem;
    border-radius: 8px;
    margin-bottom: 2rem;
    border: 1px solid #e5e5e5;
}

.bulk-upload-instructions h3 {
    margin-bottom: 1rem;
    color: #1a1a1a;
}

.bulk-upload-instructions ul {
    margin: 1rem 0;
    padding-left: 2rem;
}

.bulk-upload-instructions code {
    background: white;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    border: 1px solid #e5e5e5;
    font-family: monospace;
}

.bulk-upload-instructions pre {
    background: white;
    padding: 1rem;
    border-radius: 6px;
    border: 1px solid #e5e5e5;
    overflow-x: auto;
    margin-top: 0.5rem;
}
</style>